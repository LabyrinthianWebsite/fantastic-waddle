<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@300;400;500;600&family=Crimson+Text:ital,wght@0,400;0,600;1,400&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Playfair Display', 'Georgia', 'Times New Roman', 'Book Antiqua', serif;
            line-height: 1.7;
            color: #2c2c2c;
            background: linear-gradient(145deg, #f8f6f0 0%, #e8e6e0 50%, #f5f3ed 100%);
            min-height: 100vh;
            position: relative;
        }

        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 20% 50%, rgba(218, 165, 32, 0.03) 0%, transparent 50%),
                        radial-gradient(circle at 80% 20%, rgba(139, 69, 19, 0.02) 0%, transparent 50%),
                        radial-gradient(circle at 40% 80%, rgba(160, 82, 45, 0.02) 0%, transparent 50%);
            pointer-events: none;
            z-index: -1;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header */
        header {
            background: rgba(248, 246, 240, 0.98);
            backdrop-filter: blur(15px);
            box-shadow: 0 2px 30px rgba(139, 69, 19, 0.08), inset 0 1px 0 rgba(218, 165, 32, 0.1);
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 1px solid rgba(139, 69, 19, 0.1);
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
        }

        .logo {
            font-size: 2.2rem;
            font-weight: 400;
            color: #8b4513;
            text-decoration: none;
            font-family: 'Playfair Display', 'Georgia', 'Times New Roman', serif;
            letter-spacing: 0.5px;
            text-shadow: 0 1px 2px rgba(139, 69, 19, 0.1);
        }

        .logo::before {
            content: '✦ ';
            color: #daa520;
            margin-right: 0.5rem;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        .nav-links a {
            color: #5d4037;
            text-decoration: none;
            font-weight: 400;
            font-family: 'Crimson Text', 'Georgia', 'Times New Roman', serif;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            padding: 0.5rem 1rem;
            border-radius: 4px;
            position: relative;
        }

        .nav-links a::before {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 50%;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #daa520, #b8860b);
            transition: all 0.3s ease;
            transform: translateX(-50%);
        }

        .nav-links a:hover, .nav-links a.active {
            color: #8b4513;
            background: rgba(218, 165, 32, 0.08);
        }

        .nav-links a:hover::before, .nav-links a.active::before {
            width: 80%;
        }

        /* Search */
        .search-box {
            position: relative;
        }

        .search-box input {
            padding: 0.7rem 1.2rem;
            border: 2px solid rgba(139, 69, 19, 0.2);
            border-radius: 30px;
            outline: none;
            width: 280px;
            transition: all 0.3s ease;
            background: rgba(248, 246, 240, 0.9);
            color: #5d4037;
            font-family: 'Crimson Text', 'Georgia', 'Times New Roman', serif;
            font-size: 1rem;
        }

        .search-box input:focus {
            border-color: #daa520;
            box-shadow: 0 0 0 3px rgba(218, 165, 32, 0.1);
            background: rgba(255, 255, 255, 0.95);
        }

        .search-box input::placeholder {
            color: rgba(93, 64, 55, 0.6);
            font-style: italic;
        }

        /* Main content */
        main {
            min-height: calc(100vh - 80px);
            padding: 2rem 0;
        }

        .content-card {
            background: linear-gradient(145deg, rgba(255, 255, 255, 0.9), rgba(248, 246, 240, 0.95));
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(139, 69, 19, 0.1), 
                        0 5px 15px rgba(139, 69, 19, 0.08),
                        inset 0 1px 0 rgba(255, 255, 255, 0.7);
            padding: 3rem;
            margin-bottom: 2.5rem;
            border: 1px solid rgba(218, 165, 32, 0.1);
            position: relative;
            overflow: hidden;
        }

        .content-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(218, 165, 32, 0.3), transparent);
        }

        /* Grid layouts */
        .grid {
            display: grid;
            gap: 1.5rem;
        }

        .grid-2 { grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); }
        .grid-3 { grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); }
        .grid-4 { grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); }

        /* Cards */
        .card {
            background: linear-gradient(145deg, rgba(255, 255, 255, 0.95), rgba(248, 246, 240, 0.9));
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 8px 25px rgba(139, 69, 19, 0.12), 
                        0 3px 10px rgba(139, 69, 19, 0.08);
            transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            border: 1px solid rgba(218, 165, 32, 0.1);
            position: relative;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 2px;
            background: linear-gradient(90deg, #daa520, #b8860b, #daa520);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(139, 69, 19, 0.18), 
                        0 8px 20px rgba(139, 69, 19, 0.12);
        }

        .card:hover::before {
            opacity: 1;
        }

        .card-image {
            width: 100%;
            height: 220px;
            object-fit: cover;
            background: linear-gradient(135deg, #f5f3ed, #e8e6e0, #f0ede5);
            position: relative;
        }

        .card-image::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(180deg, 
                transparent 0%, 
                rgba(139, 69, 19, 0.02) 50%, 
                rgba(139, 69, 19, 0.05) 100%);
        }

        .card-content {
            padding: 2rem;
        }

        .card-title {
            font-size: 1.3rem;
            font-weight: 400;
            margin-bottom: 0.7rem;
            color: #5d4037;
            font-family: 'Playfair Display', 'Georgia', 'Times New Roman', serif;
            letter-spacing: 0.3px;
        }

        .card-meta {
            color: #8d6e63;
            font-size: 0.95rem;
            margin-bottom: 1.2rem;
            font-family: 'Crimson Text', 'Georgia', 'Times New Roman', serif;
            font-style: italic;
        }

        .card-description {
            color: #6d4c41;
            line-height: 1.6;
            font-family: 'Crimson Text', 'Georgia', 'Times New Roman', serif;
        }

        /* Buttons */
        .btn {
            display: inline-block;
            padding: 0.8rem 2rem;
            background: linear-gradient(135deg, #daa520, #b8860b);
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 400;
            font-family: 'Crimson Text', 'Georgia', 'Times New Roman', serif;
            font-size: 1.05rem;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
            box-shadow: 0 4px 12px rgba(218, 165, 32, 0.3);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .btn:hover {
            background: linear-gradient(135deg, #b8860b, #996f06);
            transform: translateY(-2px);
            box-shadow: 0 6px 18px rgba(218, 165, 32, 0.4);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #8d6e63, #6d4c41);
            box-shadow: 0 4px 12px rgba(141, 110, 99, 0.3);
        }

        .btn-secondary:hover {
            background: linear-gradient(135deg, #6d4c41, #5d4037);
            box-shadow: 0 6px 18px rgba(141, 110, 99, 0.4);
        }

        /* Stats */
        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 1.5rem;
            margin-bottom: 3rem;
        }

        .stat-card {
            background: linear-gradient(135deg, 
                rgba(218, 165, 32, 0.1) 0%, 
                rgba(184, 134, 11, 0.05) 50%, 
                rgba(218, 165, 32, 0.08) 100%);
            color: #5d4037;
            padding: 2.5rem 2rem;
            border-radius: 16px;
            text-align: center;
            border: 1px solid rgba(218, 165, 32, 0.2);
            box-shadow: 0 8px 25px rgba(139, 69, 19, 0.1);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(218, 165, 32, 0.03) 0%, transparent 70%);
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 35px rgba(139, 69, 19, 0.15);
        }

        .stat-card:hover::before {
            opacity: 1;
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 300;
            display: block;
            color: #8b4513;
            font-family: 'Playfair Display', 'Georgia', 'Times New Roman', serif;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            font-family: 'Crimson Text', 'Georgia', 'Times New Roman', serif;
            font-size: 1.1rem;
            color: #6d4c41;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 400;
        }

        .stat-label {
            opacity: 0.9;
            font-size: 0.9rem;
        }

        /* Footer */
        footer {
            background: linear-gradient(135deg, rgba(248, 246, 240, 0.98), rgba(240, 237, 229, 0.95));
            backdrop-filter: blur(15px);
            text-align: center;
            padding: 3rem 0;
            margin-top: 4rem;
            color: #6d4c41;
            border-top: 1px solid rgba(218, 165, 32, 0.2);
            position: relative;
        }

        footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 50%;
            width: 100px;
            height: 1px;
            background: linear-gradient(90deg, transparent, #daa520, transparent);
            transform: translateX(-50%);
        }

        footer p {
            font-family: 'Crimson Text', 'Georgia', 'Times New Roman', serif;
            font-size: 1rem;
            letter-spacing: 0.5px;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .nav-links {
                display: none;
            }
            
            .search-box input {
                width: 200px;
            }
            
            .container {
                padding: 0 15px;
            }
        }

        /* Page specific styles */
        .hero {
            text-align: center;
            margin-bottom: 4rem;
            padding: 4rem 0;
            position: relative;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 100px;
            height: 1px;
            background: linear-gradient(90deg, transparent, #daa520, transparent);
            transform: translate(-50%, -50%);
            z-index: -1;
        }

        .hero h1 {
            font-size: 4rem;
            color: #8b4513;
            margin-bottom: 1.5rem;
            text-shadow: 0 2px 4px rgba(139, 69, 19, 0.1);
            font-family: 'Playfair Display', 'Georgia', 'Times New Roman', serif;
            font-weight: 300;
            letter-spacing: 1px;
            position: relative;
        }

        .hero h1::after {
            content: '';
            position: absolute;
            bottom: -10px;
            left: 50%;
            width: 60px;
            height: 2px;
            background: linear-gradient(90deg, #daa520, #b8860b);
            transform: translateX(-50%);
        }

        .hero p {
            font-size: 1.4rem;
            color: #6d4c41;
            margin-bottom: 2.5rem;
            font-family: 'Crimson Text', 'Georgia', 'Times New Roman', serif;
            font-style: italic;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.6;
        }

        .section-title {
            font-size: 2.5rem;
            color: #5d4037;
            margin-bottom: 2rem;
            text-align: center;
            font-family: 'Playfair Display', 'Georgia', 'Times New Roman', serif;
            font-weight: 300;
            letter-spacing: 0.5px;
            position: relative;
        }

        .section-title::before {
            content: '❖';
            position: absolute;
            left: 50%;
            top: -30px;
            transform: translateX(-50%);
            color: #daa520;
            font-size: 1.5rem;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -15px;
            left: 50%;
            width: 80px;
            height: 1px;
            background: linear-gradient(90deg, transparent, #daa520, transparent);
            transform: translateX(-50%);
        }

        .thumbnail {
            width: 100%;
            height: 150px;
            object-fit: cover;
            border-radius: 8px;
        }

        .model-grid .card-image {
            height: 250px;
        }

        .set-grid .card-image {
            height: 180px;
        }
    </style>
</head>
<body>
    <header>
        <nav class="container">
            <a href="/" class="logo">Gallery Suite</a>
            <ul class="nav-links">
                <li><a href="/" class="<%= typeof page !== 'undefined' && page === 'home' ? 'active' : '' %>">Home</a></li>
                <li><a href="/studios" class="<%= typeof page !== 'undefined' && page === 'studios' ? 'active' : '' %>">Studios</a></li>
                <li><a href="/models" class="<%= typeof page !== 'undefined' && page === 'models' ? 'active' : '' %>">Sites/Models</a></li>
                <li><a href="/sets" class="<%= typeof page !== 'undefined' && page === 'sets' ? 'active' : '' %>">Sets</a></li>
                <li><a href="/admin" class="<%= typeof page !== 'undefined' && page === 'admin' ? 'active' : '' %>">Admin</a></li>
            </ul>
            <div class="search-box">
                <form action="/search" method="GET">
                    <input type="text" name="q" placeholder="Search galleries..." value="<%= typeof query !== 'undefined' ? query : '' %>">
                </form>
            </div>
        </nav>
    </header>

    <main>
        <div class="container">
            <div class="hero">
                <h1>Gallery Suite</h1>
                <p>Curating Excellence in Digital Collections • Professional Media Archival for Discerning Institutions</p>
            </div>

            <div class="stats">
                <div class="stat-card">
                    <span class="stat-number"><%= stats.studios.count %></span>
                    <span class="stat-label">Studios</span>
                </div>
                <div class="stat-card">
                    <span class="stat-number"><%= stats.models.count %></span>
                    <span class="stat-label">Sites/Models</span>
                </div>
                <div class="stat-card">
                    <span class="stat-number"><%= stats.sets.count %></span>
                    <span class="stat-label">Sets</span>
                </div>
                <div class="stat-card">
                    <span class="stat-number"><%= stats.media.count %></span>
                    <span class="stat-label">Media Files</span>
                </div>
            </div>

            <% if (studios.length > 0) { %>
            <div class="content-card">
                <h2 class="section-title">Featured Studios</h2>
                <div class="grid grid-3">
                    <% studios.slice(0, 6).forEach(studio => { %>
                    <div class="card">
                        <% if (studio.logo_thumb_path) { %>
                        <img src="/<%= studio.logo_thumb_path %>" alt="<%= studio.name %>" class="card-image">
                        <% } else { %>
                        <div class="card-image" style="background: linear-gradient(45deg, #667eea, #764ba2); display: flex; align-items: center; justify-content: center; color: white; font-size: 1.5rem; font-weight: bold;">
                            <%= studio.name.substring(0, 2).toUpperCase() %>
                        </div>
                        <% } %>
                        <div class="card-content">
                            <h3 class="card-title"><%= studio.name %></h3>
                            <div class="card-meta">
                                <%= studio.model_count %> models • <%= studio.set_count %> sets
                            </div>
                            <% if (studio.description) { %>
                            <p class="card-description"><%= studio.description.substring(0, 100) %>...</p>
                            <% } %>
                            <a href="/studios/<%= studio.slug %>" class="btn" style="margin-top: 1rem;">View Studio</a>
                        </div>
                    </div>
                    <% }); %>
                </div>
                <div style="text-align: center; margin-top: 2rem;">
                    <a href="/studios" class="btn btn-secondary">View All Studios</a>
                </div>
            </div>
            <% } %>

            <% if (models.length > 0) { %>
            <div class="content-card">
                <h2 class="section-title">Featured Sites/Models</h2>
                <div class="grid grid-4 model-grid">
                    <% models.forEach(model => { %>
                    <div class="card">
                        <% if (model.profile_thumb_path) { %>
                        <img src="/<%= model.profile_thumb_path %>" alt="<%= model.name %>" class="card-image">
                        <% } else { %>
                        <div class="card-image" style="background: linear-gradient(45deg, #764ba2, #667eea); display: flex; align-items: center; justify-content: center; color: white; font-size: 1.2rem; font-weight: bold;">
                            <%= model.name.substring(0, 2).toUpperCase() %>
                        </div>
                        <% } %>
                        <div class="card-content">
                            <h3 class="card-title"><%= model.name %></h3>
                            <div class="card-meta">
                                <% if (model.studio_name) { %>
                                <%= model.studio_name %> • 
                                <% } %>
                                <%= model.set_count %> sets
                            </div>
                            <a href="/models/<%= model.slug %>" class="btn" style="margin-top: 1rem;">View Profile</a>
                        </div>
                    </div>
                    <% }); %>
                </div>
                <div style="text-align: center; margin-top: 2rem;">
                    <a href="/models" class="btn btn-secondary">View All Sites/Models</a>
                </div>
            </div>
            <% } %>

            <% if (recentSets.length > 0) { %>
            <div class="content-card">
                <h2 class="section-title">Recent Sets</h2>
                <div class="grid grid-3 set-grid">
                    <% recentSets.forEach(set => { %>
                    <div class="card">
                        <% if (set.cover_thumb_path) { %>
                        <img src="/<%= set.cover_thumb_path %>" alt="<%= set.name %>" class="card-image">
                        <% } else { %>
                        <div class="card-image" style="background: linear-gradient(45deg, #f093fb, #f5576c); display: flex; align-items: center; justify-content: center; color: white; font-size: 1rem; font-weight: bold;">
                            <%= set.name.substring(0, 3).toUpperCase() %>
                        </div>
                        <% } %>
                        <div class="card-content">
                            <h3 class="card-title"><%= set.name %></h3>
                            <div class="card-meta">
                                <a href="/models/<%= set.model_slug %>"><%= set.model_name %></a>
                                <% if (set.studio_name) { %>
                                • <a href="/studios/<%= set.studio_slug %>"><%= set.studio_name %></a>
                                <% } %>
                            </div>
                            <div class="card-meta">
                                <%= set.image_count || 0 %> images • <%= set.video_count || 0 %> videos
                            </div>
                            <a href="/sets/<%= set.slug %>" class="btn" style="margin-top: 1rem;">View Set</a>
                        </div>
                    </div>
                    <% }); %>
                </div>
                <div style="text-align: center; margin-top: 2rem;">
                    <a href="/sets" class="btn btn-secondary">View All Sets</a>
                </div>
            </div>
            <% } %>

        </div>
    </main>

    <footer>
        <div class="container">
            <p>✦ Gallery Suite ✦ • A Digital Collection Management System • &copy; 2024</p>
        </div>
    </footer>

    <script>
        // Simple search enhancement
        document.querySelector('.search-box input').addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                this.form.submit();
            }
        });

        // Card hover effects
        document.querySelectorAll('.card').forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.transform = 'translateY(-5px)';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.transform = 'translateY(0)';
            });
        });
    </script>
</body>
</html>