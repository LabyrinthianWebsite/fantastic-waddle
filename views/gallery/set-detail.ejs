<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header */
        header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: #667eea;
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        .nav-links a {
            text-decoration: none;
            color: #333;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .nav-links a:hover {
            color: #667eea;
        }

        /* Main Content */
        main {
            padding: 2rem 0;
        }

        .breadcrumb {
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 2rem;
        }

        .breadcrumb a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
        }

        .breadcrumb a:hover {
            color: white;
        }

        /* Set Header */
        .set-header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }

        .set-info {
            display: grid;
            grid-template-columns: auto 1fr auto;
            gap: 2rem;
            align-items: center;
        }

        .set-icon {
            width: 80px;
            height: 80px;
            border-radius: 15px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 2rem;
        }

        .set-details h1 {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .set-meta {
            color: #666;
            font-size: 1.1rem;
            margin-bottom: 1rem;
        }

        .set-description {
            color: #555;
            line-height: 1.6;
        }

        .set-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(100px, 1fr));
            gap: 1rem;
            text-align: center;
        }

        .stat-item {
            background: rgba(102, 126, 234, 0.1);
            padding: 1rem;
            border-radius: 10px;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: bold;
            color: #667eea;
        }

        .stat-label {
            color: #666;
            font-size: 0.9rem;
        }

        /* Tags Section */
        .tags-section {
            margin-bottom: 2rem;
        }

        .tags-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 1.5rem 2rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }

        .tags-title {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: #333;
        }

        .tags-list {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
        }

        .tag {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 500;
        }

        /* Media Grid */
        .media-section {
            margin-top: 2rem;
        }

        .section-header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px 15px 0 0;
            padding: 1.5rem 2rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        }

        .section-title {
            font-size: 1.8rem;
            margin-bottom: 0.5rem;
            color: #333;
        }

        .section-subtitle {
            color: #666;
        }

        .media-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 1.5rem;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            padding: 2rem;
            border-radius: 0 0 15px 15px;
        }

        .media-item {
            background: white;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .media-item:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }

        .media-preview {
            width: 100%;
            height: 200px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
            position: relative;
        }

        .media-preview img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .media-type-indicator {
            position: absolute;
            top: 0.5rem;
            right: 0.5rem;
            background: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.8rem;
        }

        .media-info {
            padding: 1rem;
        }

        .media-filename {
            font-weight: 500;
            margin-bottom: 0.5rem;
            word-break: break-word;
        }

        .media-meta {
            color: #666;
            font-size: 0.9rem;
        }

        .btn {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.3s ease;
            display: inline-block;
            margin-top: 0.5rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
        }

        /* Empty State */
        .empty-state {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 4rem 2rem;
            text-align: center;
            color: #666;
        }

        .empty-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .set-info {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 1rem;
            }

            .set-stats {
                grid-template-columns: repeat(2, 1fr);
            }

            .media-grid {
                grid-template-columns: 1fr;
            }

            .nav-links {
                display: none;
            }

            .tags-list {
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav class="container">
            <a href="/" class="logo">Gallery Suite</a>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/studios">Studios</a></li>
                <li><a href="/models">Models</a></li>
                <li><a href="/sets">Sets</a></li>
            </ul>
        </nav>
    </header>

    <main class="container">
        <div class="breadcrumb">
            <a href="/">Home</a> › 
            <a href="/sets">Sets</a> › 
            <%= set.name %>
        </div>

        <div class="set-header">
            <div class="set-info">
                <div class="set-icon">
                    📸
                </div>
                <div class="set-details">
                    <h1><%= set.name %></h1>
                    <div class="set-meta">
                        <% if (set.model_name) { %>
                            Model: <a href="/models/<%= set.model_slug %>"><%= set.model_name %></a>
                        <% } %>
                        <% if (set.studio_name) { %>
                            • Studio: <a href="/studios/<%= set.studio_slug %>"><%= set.studio_name %></a>
                        <% } %>
                        <% if (set.created_at) { %>
                            • Created <%= new Date(set.created_at).toLocaleDateString() %>
                        <% } %>
                    </div>
                    <% if (set.description) { %>
                        <div class="set-description">
                            <%= set.description %>
                        </div>
                    <% } %>
                </div>
                <div class="set-stats">
                    <div class="stat-item">
                        <div class="stat-number"><%= media ? media.length : 0 %></div>
                        <div class="stat-label">Media</div>
                    </div>
                    <% if (tags && tags.length > 0) { %>
                        <div class="stat-item">
                            <div class="stat-number"><%= tags.length %></div>
                            <div class="stat-label">Tags</div>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>

        <% if (tags && tags.length > 0) { %>
            <div class="tags-section">
                <div class="tags-container">
                    <h3 class="tags-title">Tags</h3>
                    <div class="tags-list">
                        <% tags.forEach(tag => { %>
                            <span class="tag"><%= tag.name %></span>
                        <% }); %>
                    </div>
                </div>
            </div>
        <% } %>

        <div class="media-section">
            <% if (media && media.length > 0) { %>
                <div class="section-header">
                    <h2 class="section-title">Media</h2>
                    <p class="section-subtitle">Browse all media in this set</p>
                </div>
                <div class="media-grid">
                    <% media.forEach(item => { %>
                        <div class="media-item">
                            <div class="media-preview">
                                <% if (item.thumbnail_path || item.file_path) { %>
                                    <img src="<%= item.thumbnail_path || item.file_path %>" alt="<%= item.filename || 'Media item' %>">
                                <% } else { %>
                                    🎬 <%= item.filename || 'Media' %>
                                <% } %>
                                <% if (item.file_type) { %>
                                    <div class="media-type-indicator">
                                        <%= item.file_type.toUpperCase() %>
                                    </div>
                                <% } %>
                            </div>
                            <div class="media-info">
                                <div class="media-filename"><%= item.filename || 'Untitled' %></div>
                                <div class="media-meta">
                                    <% if (item.file_size) { %>
                                        Size: <%= Math.round(item.file_size / 1024) %> KB
                                    <% } %>
                                    <% if (item.created_at) { %>
                                        • <%= new Date(item.created_at).toLocaleDateString() %>
                                    <% } %>
                                </div>
                                <% if (item.file_path) { %>
                                    <a href="<%= item.file_path %>" class="btn btn-primary" target="_blank">View</a>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                </div>
            <% } else { %>
                <div class="empty-state">
                    <div class="empty-icon">📸</div>
                    <h3>No Media Yet</h3>
                    <p>No photos or videos have been added to this set yet.</p>
                </div>
            <% } %>
        </div>
    </main>
</body>
</html>