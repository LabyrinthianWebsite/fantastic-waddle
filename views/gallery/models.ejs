<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            color: #333;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header */
        header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.1);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 0;
        }

        .logo {
            font-size: 1.8rem;
            font-weight: bold;
            color: #667eea;
            text-decoration: none;
        }

        .nav-links {
            display: flex;
            list-style: none;
            gap: 2rem;
        }

        .nav-links a {
            text-decoration: none;
            color: #333;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .nav-links a:hover {
            color: #667eea;
        }

        /* Main Content */
        main {
            padding: 2rem 0;
        }

        .breadcrumb {
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 2rem;
        }

        .breadcrumb a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
        }

        .breadcrumb a:hover {
            color: white;
        }

        /* Page Header */
        .page-header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
        }

        .page-title {
            font-size: 2.5rem;
            margin-bottom: 0.5rem;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .page-subtitle {
            color: #666;
            font-size: 1.1rem;
            margin-bottom: 1.5rem;
        }

        /* Filters */
        .filters {
            display: flex;
            gap: 1rem;
            align-items: center;
            flex-wrap: wrap;
        }

        .filter-group {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .search-input {
            flex: 1;
            min-width: 250px;
            padding: 0.5rem 1rem;
            border: 1px solid #ddd;
            border-radius: 6px;
            background: white;
            color: #333;
            font-size: 0.9rem;
        }

        .search-input:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        .filter-label {
            font-weight: 500;
            color: #333;
        }

        .filter-select {
            padding: 0.5rem 1rem;
            border: 1px solid #ddd;
            border-radius: 6px;
            background: white;
            color: #333;
            font-size: 0.9rem;
            cursor: pointer;
        }

        .filter-select:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
        }

        /* Models Grid */
        .models-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .model-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .model-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }

        .model-avatar {
            width: 100%;
            height: 200px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 3rem;
            font-weight: bold;
        }

        .model-content {
            padding: 1.5rem;
        }

        .model-name {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
            color: #333;
        }

        .model-studio {
            color: #667eea;
            font-size: 0.9rem;
            margin-bottom: 1rem;
            text-decoration: none;
        }

        .model-studio:hover {
            text-decoration: underline;
        }

        .model-stats {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .stat-item {
            text-align: center;
            background: rgba(102, 126, 234, 0.1);
            padding: 1rem;
            border-radius: 8px;
        }

        .stat-number {
            font-size: 1.5rem;
            font-weight: bold;
            color: #667eea;
        }

        .stat-label {
            color: #666;
            font-size: 0.8rem;
        }

        .model-actions {
            display: flex;
            gap: 0.5rem;
        }

        .btn {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.3s ease;
            display: inline-block;
            text-align: center;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            flex: 1;
        }

        .btn-primary:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(102, 126, 234, 0.3);
        }

        .btn-outline {
            background: transparent;
            border: 1px solid #667eea;
            color: #667eea;
        }

        .btn-outline:hover {
            background: #667eea;
            color: white;
        }

        .btn-secondary {
            background: #6c757d;
            color: white;
        }

        .btn-secondary:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(108, 117, 125, 0.3);
        }

        /* Pagination */
        .pagination {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 0.5rem;
            margin: 3rem 0 2rem;
            flex-wrap: wrap;
        }

        .pagination-info {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 10px;
            padding: 1rem;
            text-align: center;
            color: #333;
            margin-bottom: 1rem;
            font-size: 1rem;
        }

        .pagination a,
        .pagination span {
            padding: 0.5rem 1rem;
            background: rgba(255, 255, 255, 0.95);
            border-radius: 8px;
            text-decoration: none;
            color: #333;
            transition: all 0.3s ease;
            min-width: 40px;
            text-align: center;
        }

        .pagination a:hover {
            background: #667eea;
            color: white;
            transform: translateY(-2px);
        }

        .pagination .active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            font-weight: bold;
        }

        .pagination .disabled {
            opacity: 0.5;
            pointer-events: none;
        }

        /* Empty State */
        .empty-state {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            padding: 4rem 2rem;
            text-align: center;
            color: #666;
        }

        .empty-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            opacity: 0.5;
        }

        /* Add Button */
        .add-button {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.5rem;
            text-decoration: none;
            box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
            transition: transform 0.3s ease;
        }

        .add-button:hover {
            transform: scale(1.1);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .models-grid {
                grid-template-columns: 1fr;
            }

            .nav-links {
                display: none;
            }

            .filters {
                flex-direction: column;
                align-items: stretch;
            }

            .filter-group {
                justify-content: space-between;
            }

            .search-input {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <header>
        <nav class="container">
            <a href="/" class="logo">Gallery Suite</a>
            <ul class="nav-links">
                <li><a href="/">Home</a></li>
                <li><a href="/studios">Studios</a></li>
                <li><a href="/models">Sites/Models</a></li>
                <li><a href="/sets">Sets</a></li>
            </ul>
        </nav>
    </header>

    <main class="container">
        <div class="breadcrumb">
            <a href="/">Home</a> › Sites/Models
        </div>

        <div class="page-header">
            <h1 class="page-title">Sites/Models</h1>
            <p class="page-subtitle">Browse all models in the gallery</p>
            
            <form class="filters" method="GET">
                <input type="text" name="search" class="search-input" placeholder="Search models..." value="<%= search || '' %>">
                <div class="filter-group">
                    <label for="studio-filter" class="filter-label">Studio:</label>
                    <select id="studio-filter" name="studio" class="filter-select">
                        <option value="">All Studios</option>
                        <% if (studios && studios.length > 0) { %>
                            <% studios.forEach(studio => { %>
                                <option value="<%= studio.id %>" <%= selectedStudio == studio.id ? 'selected' : '' %>>
                                    <%= studio.name %>
                                </option>
                            <% }); %>
                        <% } %>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary">Apply</button>
                <% if (search || selectedStudio) { %>
                    <a href="/models" class="btn btn-secondary">Clear</a>
                <% } %>
            </form>
        </div>

        <% if (models && models.length > 0) { %>
            <div class="pagination-info">
                Showing <%= models.length %> of <%= totalModels %> models
            </div>
            <div class="models-grid">
                <% models.forEach(model => { %>
                    <div class="model-card">
                        <div class="model-avatar">
                            <% if (model.avatar_path) { %>
                                <img src="<%= model.avatar_path %>" alt="<%= model.name %>" style="width: 100%; height: 100%; object-fit: cover;">
                            <% } else { %>
                                <%= model.name.charAt(0).toUpperCase() %>
                            <% } %>
                        </div>
                        <div class="model-content">
                            <h3 class="model-name"><%= model.name %></h3>
                            <% if (model.studio_name) { %>
                                <a href="/studios/<%= model.studio_slug %>" class="model-studio">
                                    <%= model.studio_name %>
                                </a>
                            <% } %>
                            <div class="model-stats">
                                <div class="stat-item">
                                    <div class="stat-number"><%= model.set_count || 0 %></div>
                                    <div class="stat-label">Sets</div>
                                </div>
                                <div class="stat-item">
                                    <div class="stat-number"><%= model.photo_count || 0 %></div>
                                    <div class="stat-label">Photos</div>
                                </div>
                            </div>
                            <div class="model-actions">
                                <a href="/models/<%= model.slug || model.id %>" class="btn btn-primary">View</a>
                                <a href="/admin/models/<%= model.id %>/edit" class="btn btn-outline">Edit</a>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>

            <% if (totalPages > 1) { %>
            <div class="pagination">
                <% 
                const buildUrl = (page) => {
                    const params = new URLSearchParams();
                    if (search) params.append('search', search);
                    if (selectedStudio) params.append('studio', selectedStudio);
                    if (page > 1) params.append('page', page);
                    return '/models' + (params.toString() ? '?' + params.toString() : '');
                };
                %>
                
                <% if (currentPage > 1) { %>
                    <a href="<%= buildUrl(currentPage - 1) %>">&laquo; Previous</a>
                <% } else { %>
                    <span class="disabled">&laquo; Previous</span>
                <% } %>

                <% 
                const startPage = Math.max(1, currentPage - 2);
                const endPage = Math.min(totalPages, currentPage + 2);
                
                if (startPage > 1) { %>
                    <a href="<%= buildUrl(1) %>">1</a>
                    <% if (startPage > 2) { %>
                        <span>...</span>
                    <% } %>
                <% } %>

                <% for (let i = startPage; i <= endPage; i++) { %>
                    <% if (i === currentPage) { %>
                        <span class="active"><%= i %></span>
                    <% } else { %>
                        <a href="<%= buildUrl(i) %>"><%= i %></a>
                    <% } %>
                <% } %>

                <% if (endPage < totalPages) { %>
                    <% if (endPage < totalPages - 1) { %>
                        <span>...</span>
                    <% } %>
                    <a href="<%= buildUrl(totalPages) %>"><%= totalPages %></a>
                <% } %>

                <% if (currentPage < totalPages) { %>
                    <a href="<%= buildUrl(currentPage + 1) %>">Next &raquo;</a>
                <% } else { %>
                    <span class="disabled">Next &raquo;</span>
                <% } %>
            </div>
            <% } %>
        <% } else { %>
            <div class="empty-state">
                <div class="empty-icon">👤</div>
                <h3>No Models Found</h3>
                <p>No models match your search criteria.</p>
                <% if (search || selectedStudio) { %>
                    <a href="/models" class="btn btn-primary" style="margin-top: 1rem;">Clear Filters</a>
                <% } else { %>
                    <a href="/admin/models/new" class="btn btn-primary" style="margin-top: 1rem;">Add First Model</a>
                <% } %>
            </div>
        <% } %>
    </main>

    <a href="/admin/models/new" class="add-button" title="Add New Model">+</a>
</body>
</html>